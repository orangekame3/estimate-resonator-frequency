[project]
name = "estimate-resonator-frequency"
version = "0.3.0"
dependencies = [
  "numpy",
  "scipy",
  "plotly",
  "kaleido",
  "matplotlib",
]
requires-python = ">=3.10"

[project.optional-dependencies]
dev = [
  "scipy-stubs",
]
